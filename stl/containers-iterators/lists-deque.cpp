#include <bits/stdc++.h>

using namespace std;

int main()
{
    // same functions as vectors
    // except it includes operations
    list<int> l;

    l.push_front(20); // time complexity is less as it uses doubly linked list
    l.emplace_front(30);


    // deque same almost push and pop _front and _back with front and back (separate alone), 
    // also has begin, end, swap, size, rbegin, rend, clear, insert, rest same
}